RELAY_HOST=relay.inkagent.dev
# Sign-in is OAuth. Configure at least one provider, both halves, then restart.
# Callback URL to register with the provider:
#   https://<RELAY_HOST>/auth/callback
INK_OAUTH_GITHUB_ID=
INK_OAUTH_GITHUB_SECRET=
INK_OAUTH_GOOGLE_ID=
INK_OAUTH_GOOGLE_SECRET=
